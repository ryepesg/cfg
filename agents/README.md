# Agent templates

This directory contains copyable, publish-safe templates for a repository's
own instruction files. It is not a global harness configuration, deployment
mechanism, skill registry, or symlink source.

Copy the templates into the project that owns the work, then give that project
an owned runtime smoke test.
